#!/bin/bash
mkdir -p "log/archive"
mkdir -p "log/incremental"
mkdir -p "log/initial"
mkdir -p "log/$(date +'%Y-%m-%d')-full"
tar -czf "log/archive/$(date +'%Y%m%d_%H%m').tar.gz" \
    log/*-full \
    log/initial \
    log/incremental \
    && rm -r log/*-full log/incremental log/initial
