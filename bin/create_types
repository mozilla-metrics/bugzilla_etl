#! /bin/bash
source etl_env.shinclude

for f in `find $KETTLE_LIBS -type f -name "*.jar"` `find $KETTLE_LIBS -type f -name "*.zip"`
do
  CLASSPATH=$CLASSPATH:$f
done

java -cp "${CLASSPATH}" com.mozilla.bugzilla_etl.lily.Types "${zookeeper_host}"
